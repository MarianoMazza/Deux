{% extends 'base.html.twig' %}

{% block panelhead %}Recuperar Contraseña{% endblock %}

{% block panelbody %}
<div class="formulario">
    <div class="main container">
        <div class="col-md-5">
            <div>
                <form action="{{ path('_recoveryPass') }}">
                    {% if invalid_email is defined %}
                        <p>El email es inválido.</p>
                    {% endif %}
                    <div class="form-group">
                        <label for="email">Email: </label>
                        <input type="text" id="email" name="email" required="required"  class="form-control" placeholder="Ingrese un correo electrónico" }  />
                    </div>
                </form>
            </div>
            <div class="form-group">
                <button type="submit" class="btn label-default">Recuperar Contraseña</button>
                <a href='{{ path('_listaPubli') }}' class="btn active">Cancelar</a>
            </div>
        </div>
    </div>
</div>

{% endblock %}
